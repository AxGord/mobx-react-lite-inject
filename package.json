{
  "name": "mobx-react-lite-inject",
  "version": "1.0.0",
  "description": "mobx-react-lite inject helper",
  "main": "inject.js",
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "mobx",
    "react",
    "mobx-react-lite"
  ],
  "author": "Alexander Gordeyko",
  "license": "MIT",
  "devDependencies": {
    "@types/react": "^17.0.3",
    "mobx-react-lite": "^3.2.0",
    "typescript": "^4.2.3"
  },
  "files": [
    "inject.d.ts",
    "inject.js"
  ]
}
